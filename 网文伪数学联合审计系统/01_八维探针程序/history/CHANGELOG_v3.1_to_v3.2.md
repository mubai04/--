# CHANGELOG：v3.1 → v3.2

## P0

- 有分无证据现在直接阻断。
- 每条八维、六门和优先问题证据必须能在正文中定位。
- 删除 Codex 可填写的判断边界字段，改由 Python 根据输入类型固定生成。
- 统一唯一主规范，并重新生成 ZIP 与 SHA-256 清单。

## P1

- 正向特征按过载风险衰减；多维高过载限制最终高等级。
- compare 默认要求修订关系文件，并校验前后正文哈希。
- 相同正文哈希默认禁止作为修订效果比较。
- 小样本强制拟合不再除零；不可交叉验证时明确输出 no_cv。
- fit 改称“拟合权重与偏置”，明确不校准阈值。
- 主线全空却标低风险现在阻断。
- 示例改为两个真实不同正文版本，不再只改评分。

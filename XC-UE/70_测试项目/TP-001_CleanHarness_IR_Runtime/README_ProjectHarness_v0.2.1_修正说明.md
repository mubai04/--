# Project Harness v0.2.1 修正说明

> 状态：干净测试项目壳 / 已接入 TP-001 IR / 已建立最小工程执行层

## 本次修正

1. 移动根目录旧输入文件到 `_legacy_root_inputs/`。
2. 明确正式输入只认 `IR/`。
3. 新增 `MANIFEST_文件权限与真源表.md`。
4. 新增 `IR/IR-99_输入完整性检查.md`。
5. 新增 `runtime/RT-00_运行时索引.md`。
6. 新增 `tests/L3_执行任务单.md`。
7. 更新 `README_执行顺序.md`。
8. 更新 `00_项目说明.md`。
9. 新增中文工程入口 `engine/TP001运行入口.py`。
10. 新增 `reports/` 工程运行报告输出区。
11. 将工程逻辑拆分为 `TP001模型.py`、`TP001读取.py`、`检查.py`、`TP001报告.py`。
12. 保留 `tp001_engine.py` 作为旧入口兼容。

## 当前判断

v0.2.1 解决了 v0.2 的主要问题，并已进入最小工程态：

- 双真源
- IR 输入完整性
- Runtime 回写
- L3 执行任务入口
- 本地权限清单
- 候选正文工程检查
- JSON / Markdown 报告输出

首个工程运行：

```text
python engine/TP001运行入口.py --run-id ENGINE-RUN-20260621-004
```

结果：

```text
reports/ENGINE-RUN-20260621-004.md
状态：pass
检查数：22
失败数：0
```

## 下一步

可以进入候选正文人工合并或二次工程检查阶段：

1. 人工确认是否合并候选 `ch01`。
2. 合并后跑正式 `ch01` 工程检查。
3. 正式跑 L1-01 / L1-02 / L1-03。
4. 失败进 L1.5。
5. 必要时调 L2。
6. L3 输出修复候选产物。
7. runtime 记录状态变化。
8. 回 L1 / L1.5 复验。

# v0.2 测试计划

必须覆盖：

1. 运行时 JSON Schema 与 `additionalProperties=false`；
2. 负分子、分子大于分母、除零与非有限数值；
3. 证据文件、引文位置、同证据复用限制；
4. 短文本不得让硬闸门直接 PASS；
5. 风险阈值无 0.9—1.0 等空档；
6. `work_id` 路径越界；
7. 章级状态机；
8. Stability 权重扰动；
9. 同范围版本比较；
10. 批处理任一失败必须返回非零状态；
11. UTF-8 BOM；
12. 六项反刷分检查完整性。
